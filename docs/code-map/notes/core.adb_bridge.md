---
kind: file
---

# core.adb_bridge

> 为 ADBLab 提供轻量的 ADB Shell 调用封装

- 路径：core/adb_bridge.py

## 类

- [[core.adb_bridge.ADBBridge]] — 封装 ADB Shell、输入、屏幕尺寸和设备列表命令
- [[core.adb_bridge.ADBInputSession]] — 维护持久化的 adb shell 会话，降低输入命令延迟

