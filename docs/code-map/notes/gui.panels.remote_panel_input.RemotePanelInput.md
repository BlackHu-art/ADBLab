---
kind: class
---

# RemotePanelInput

- 模块：[[gui.panels.remote_panel_input]]
- 全名：gui.panels.remote_panel_input.RemotePanelInput

> 组合进 RemotePanel 的输入执行控制器，通过 ``self._frame`` 访问面板

## 方法

- [[gui.panels.remote_panel_input.RemotePanelInput.__init__]] — （无 docstring）
- [[gui.panels.remote_panel_input.RemotePanelInput._submit_remote_input]] — 遥控输入放入单线程队列，并把队列状态回写到 UI
- [[gui.panels.remote_panel_input.RemotePanelInput._mark_remote_submitted]] — （无 docstring）
- [[gui.panels.remote_panel_input.RemotePanelInput._mark_remote_completed]] — （无 docstring）
- [[gui.panels.remote_panel_input.RemotePanelInput._remote_input_succeeded]] — （无 docstring）
- [[gui.panels.remote_panel_input.RemotePanelInput._emit_remote_queue_status]] — （无 docstring）
- [[gui.panels.remote_panel_input.RemotePanelInput._update_remote_queue_status]] — （无 docstring）
- [[gui.panels.remote_panel_input.RemotePanelInput._send_keyevent]] — （无 docstring）
- [[gui.panels.remote_panel_input.RemotePanelInput._send_remote_action]] — （无 docstring）
- [[gui.panels.remote_panel_input.RemotePanelInput._warm_remote_input_session]] — （无 docstring）

